services:
  postgres:
    image: postgres:16
    container_name: lubhub-postgres
    environment:
      POSTGRES_DB: lubhub
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - lubhub-network

  redis:
    image: redis:7
    container_name: lubhub-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - lubhub-network

  rabbitmq:
    image: rabbitmq:3-management
    container_name: lubhub-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    networks:
      - lubhub-network

volumes:
  postgres-data:
  redis-data:
  rabbitmq-data:

networks:
  lubhub-network:
    driver: bridge