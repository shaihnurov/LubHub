# Server-side (for Next.js rewrites proxy)
API_BASE_URL=http://localhost:5217

# Client-side (for SignalR WebSocket — cannot be proxied through rewrites)
NEXT_PUBLIC_SIGNALR_URL=http://localhost:5217
